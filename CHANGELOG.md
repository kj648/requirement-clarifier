# Changelog

## [2.10.0] - 2026-08-28

**确认单换肤（Claude Design 复刻）。** 视觉与交互按设计稿重做，功能契约一条未动
（`__QUESTIONNAIRE_DATA__` 注入、i18n 外壳、reveal/na/carry/clash、机读回执、
noscript/bootfail、print 全部保留，189 条测试与模板 JS 机检全绿）：

- 记账凭证纸 → 白卡蓝墨：灰底白卡、8px 圆角浅影、蓝为主动作色、红只留给警示；
  代码坐标改靛蓝底与选项蓝区分
- 题号索引改为左侧悬浮窄条（58px 常驻、悬停/键盘聚焦展开 272px），新增第一部分
  （P1）入口与核对进度分数；窄屏退回把手呼出的浮层
- 进度条改吸顶毛玻璃浮卡；「怎么填」拆成 01–04 编号双栏（i18n 新增
  howto_label/howto_1..4，中英同增）
- 第一部分从表格改为卡片行 + 对/不对分段按钮（输入 name/id 不变，回执与机检无感）
- 题卡：pill 标签、答后蓝色内侧描边、演示数字命中行蓝色点亮；导出条从吸底改为随文卡片；
  回执弹窗按设计稿重排（统计卡 + 逐题清单 + 折叠原始文本）

**去仪式化。** 确认单是人与人的交流——开发者自己知道找谁确认，回执回来即结论（维护者裁定）：

- 「复制回执」只复制给 AI 的机读内容（标题 + JSON 块的最小形态，机检结论与完整 md 逐字
  相同）；「下载 .md」保留做完整存档
- 落款仪式整体拆除：填写人/部门输入框、未署名警示、【开发拟定·待追认】降级标签、机检的
  署名规则全部废除；保留导出时间（自动）。纪律内核不动：沉默≠同意（标签只因业务在回执里
  过目转正）、回执归档进 raw/ 溯源、三档标签、阻塞级不许用【开发拟定】顶过去
- 阶段三明确：机检是 AI 自己跑的动作，绝不指示用户去本地运行脚本——用户的动作只有一个，
  把业务的回复交给 AI
- 流程只出 HTML，`--md` 降为不宣传的工具能力

## [2.8.0] - 2026-08-28

**英文产出落地（#1 收尾）。** `doc.lang: "zh" | "en"`——页面外壳与 md 骨架走同一份
locale 表（`scripts/i18n.py`，189 键 ×2），题目内容本就由 AI 按交互语言写入 json。
协议层（机读区键名、三档标签）不翻译。英文确认单浏览器实测 CJK 计数为 0。

**机检语义化收口。** 选项 `kind`（dontknow/nonexistent/other）随回执导出，规则 3 与
「需要下一轮」信号从关键词猜测变结构判定；`第一部分[].核对` 导出机器枚举 ok/no/mute
（规则 4 语言无关）；md 末尾附锚点表——checker 据表把英文结构词归一为中文规范词后跑
原判据，中文是恒等映射（正文一字不变，有测试焊死）；锚点表被手改坏时降级为恒等映射
并告警，不再崩溃。

**schema 双真源焊死。** `questionnaire.schema.json` 的 required 与 `validate()` 的
`DOC_FIELDS`/`Q_FIELDS` 用测试锁定，分叉即红。

**License：PolyForm NC → MIT。** README 加十秒版 Before/After，模式表分核心/进阶，
Topics 增至 14。

## [2.7.0] - 2026-08-28

**回执机检结构化优先（多语言的地基）。** HTML 导出的回执自带机读 JSON 区，机检现在优先按
它判——语言无关；手写/旧回执走中文锚点兼容层；机读区损坏则降级并告警。英文回执从「九条
规则静默全灭」变为 7/9 条照常触发。机读区新增 `阻塞` 字段。（#1 的 P0）

**样例业务主题整体替换：逾期提醒 → 报销打款。** 29 个文件换血（桩件、规则文档、原话归档、
回执、spec、7 份 golden fixture、CI 硬路径），四题的证据档位与联动结构一一保留，坐标逐行
重新对齐，全仓不再出现「逾期/催收/还款」（历史归档除外）。

**确认单版式改版。** 题号索引改为最左侧悬浮栏，可收起（把手常驻、localStorage 记忆、
窄窗默认收起、打印隐藏）；正文取消两栏，抬头/说明/状态条/题卡/落款全部等宽对齐。

**进入分发阶段。** README 改双语（英文主入口 + 中文完整版 README.zh-CN.md）、真实截图、
最短安装路径 `npx skills add kj648/requirement-clarifier`（实测 17 种 harness）、GitHub
About/Topics、v2.6.0 首个 Release。SKILL.md description 补英文触发句。

## [2.6.0] - 2026-08-27

**HTML 可交互确认单。** 阶段二的确认单从 Markdown 换成单文件 HTML——业务点选即填，
不用碰 markdown。题目数据是 `questionnaire.json`，交互写在固定模板里，一次写好全项目复用。

- 依赖从散文注释（"若第 1 题选 A，第 3、4 题可跳过"）变成声明式 `layer` + `links`，
  由页面运行时执行；悬空引用、反向依赖、分支不对称在出包前 FAIL
- 每题挂三档依据（原话／代码／无据），无据题标红且给业务一键证伪出口
- 跨题联动：自动不适用、事实传播、**矛盾当场暴露**——把冲突检测从阶段三提前到填写现场
- 每题必有「都不是」兜底出口：一次性发单没有 AI 追问的机会，选项集猜错时业务得有路可走
- 第一部分改逐条三态核对，取消自相矛盾的全局「无异议」
- 落款：日期自动，填写人可留空但自动降级为【开发拟定·待追认】
- 机检加三条：业务证伪单独计数、未署名降级、矛盾段告警（未附说明则 FAIL）
- **角色收成开发／业务两档**：每题标 `decide: biz|dev`——biz=业务必须自己定（记
  【业务确认】），dev=开发已拟默认规则请业务过目（记【开发拟定】）。阻塞级不许标 dev。
  给谁去问是开发的责任，单子里不体现具体收件人
- **撤销「按建议回答人分区」**：真实用法是开发或产品拿着单子自己去找人确认——给谁是
  开发的事，不是单子的机制。原来的身份选择器换成「全部／必须业务定／只需过目」的硬活
  筛选，只改显隐、不折叠任何题
- **单子上不写回填期限**；删除「业务沉默协议」整节与「超时临时生效」第四档标签，
  标签体系回到三档。承重的两句并入铁律 4 与落款检查
- CI 从"只在 release 验 frontmatter"改为 push/PR 跑全套：frontmatter 校验、单元测试、
  样例确认单校验与出包、样例 spec 证据核验、旧回执回归、**模板 JS 语法与 whenToDom
  契约检查**（后者抠出模板 `<script>` 交 node，Python 测试照不出这两类盲区）
- `build_questionnaire.py` 的 `--root` 改为**从 json 位置往上找含 `docs/requirements/`
  的那一层**自动推断，降级为显式覆盖——原来每个调用点都得记得手动传，一个任务里就漏了两次

合并前修复（同一版内）——两条产物的契约分叉与两处静默失效的闸门：

- **md 路径的未作答检测修复**：子问提示行原先写在【作答区】之后，机检把模板自带的
  提示文字当成实质作答——凡带子问组的题在 md 路径上**永远**判为已作答（阻塞级漏的
  是 FAIL 而不是 WARN）。第一部分的提示语同类。空白单从报 3 道未答变成报全部 4 道
- **落款字段值不再跨字段捕获**：空的「填写人」原先会捕到下一个字段的标签名「部门」，
  非空即视为已署名——未署名降级（本版承重的那条纪律）静默失效。同时把导出器写的
  「（未填）」归为空，部门告警不再是死规则
- **第一部分核对判据改为数「未表态」条数**：导出器无论核对与否都会写满该列，
  「有没有字」的判据对 HTML 回执永远零告警
- **md 无条件渲染 `cost` 与 `demo`**：原先按 `advice_allowed` 过滤 `cost`（管建议措辞的
  开关被当成管代价的开关），规则／账务口径题拿到的是一张剥掉全部代价的决策单；
  `demo` 则整个不进 md。第一部分同步改三态，与 HTML 侧同契约
- **`blocking:true` 缺 `demo` 出包时拒收**：这条硬要求原先没有闸门，样例自己两道阻塞题
  都没配对照表
- **`validate()` 兑现「不抛异常」**：`reviewed.diffs` 写人话、`reviewed`／`evidence`／
  `demo` 非对象、`cites` 是字符串数组，原先直接 traceback；schema 的 13 项 `required`
  补齐运行时存在性检查（原先缺字段是 validate PASS → render KeyError），
  `no`／`layer` 强制整数（字符串题号会让 clash 静默不挂载）
- **HTML 加 `<noscript>` 与 boot 兜底**：脚本没跑起来时页面上只剩 124 字外壳，无标题、
  无题目、无提示；现在给业务一句能自己往下走的话
- `--md` 改为带值参数，且只给 `--md` 时不再额外往 CWD 扔一个 67KB HTML
- `.skill` 打包排除 `docs/superpowers/*`（内部设计产物，留着已撤销的回填期限）与
  `tests/__pycache__`；README 与 release.yml 的排除列表对齐
- **回执机检升级为结构化优先**：九条规则原先全按中文字面 grep（`### 问题 N：`、
  `【作答区】`、`☒ 本题不成立`、`☑ 未表态`……），回执一换语言——或者 AI 应用户要求
  把回执翻译了——九条规则不报错、只是**全部不触发**。现在 `check_file()` 分两层：带
  机读 JSON 区（HTML 导出的都带）按结构判，语言无关；无机读区的手写单/旧回执走原来的
  中文锚点，判据一字未动；机读区被手改坏则降级走锚点并明说「这次是按人读文本判的」。
  模板 `buildReceipt()` 的机读区补 `阻塞` 字段——缺它时「阻塞级未答 → FAIL」只能降成
  一律 WARN，是拦不住人的静默失效，另立机检闸门盯着

设计与借鉴取舍见 `docs/superpowers/specs/2026-08-27-html-questionnaire-design.md`。

## [2.5.0] - 2026-07-16

### Added

- **`tests/` 压测场景**：判断题规则的回归测试（superpowers 式 RED-GREEN）。3 个场景：信源分级（开发理解 vs 需求方 Excel）、环境自检（无代码库编造现状）、回执验收（脏答案直接合并）。每个含场景 prompt、基线预期违规、合规判定清单。场景 1 附真实基线实录（Opus 4.8 禁用 skill）——失败模式不是无脑顺从，而是用"风险留痕、默认已选不阻塞"话术把替需求方拍板包装成带风险开发。
- **`references/cold-start.md`**：冷启动导入。项目中途引入 skill 时的存量材料五步导入法；核心原则：存量口头共识无落款，一律先【假设】、经补签核对表批量转正。
- **验收用例桥**：`spec-template.md` 验收标准升级为业务可核对的白话用例表（当…做…应看到…，锚定决定编号，含反向用例，交付逐条打勾），闭环从开工前延伸到交付时。示例 spec 同步演示。

### Changed

- `SKILL.md`（v2.5）：
  - **信源冲突堵漏**（来自场景 1 基线压测的直接发现）：明确信源冲突属阻塞级，禁止以"按我的理解先做、风险留痕""默认已选、不阻塞开发"绕过回问需求方。
  - 持久文件区新增 context.md 归档规则（约 300 行阈值，旧决定移入 context-archive.md）。
  - 模式路由表新增冷启动导入入口；参考文件区补充 cold-start.md。

## [2.4.0] - 2026-07-16

### Added

- **`scripts/check_questionnaire.py`**：确认单回执机检。阶段三验收答案前先跑——机判未答题（阻塞级未答 FAIL）、多选提示、"我不清楚"未给知情人、第一部分未核对、落款缺失（填写人/日期缺失 FAIL）、模板内部注释残留。机检通过 ≠ 验收完成，成色分级与冲突检测仍由 AI 完成。
- **`examples/demo-project/`**：完整走查案例（逾期提醒）。从一句微信语音到可开工 spec 的全链路缩微版，含脏回答处理（新需求剥离进 parking、"看着办"落为【开发拟定】、代签风险挂待办），spec 通过 `verify_evidence.py` 核验（7 PASS / 0 FAIL），回执通过 `check_questionnaire.py` 机检。

### Changed

- `scripts/verify_evidence.py` 新增第 5 项机判：【业务确认】所在行缺日期 → WARN，`--strict` 下计为 FAIL（补齐铁律 4"业务确认必须带日期"的机检兜底）。
- `SKILL.md` 阶段三接入回执机检步骤；参考文件区补充新脚本与示例目录。

## [2.3.0] - 2026-07-16

### Changed

- 同步 `SKILL.md` 正文到 v2.3（2.1/2.2 为本地迭代版本，未单独发布）：
  - **信源分级**：铁律 3 扩展为"不替不够格的信源背书"——使用者本人的理解/推断/架构阐述一律按【假设】处理，权重低于需求方原始材料；模式 A 阶段一新增"输入分级 + 信源冲突检测"前置步骤；阶段三冲突检测扩展为三类信源比对。
  - **环境能力自检**：模式 B 新增第 0 步（不假设有代码库，按"有代码/用户口述/回问业务"三档降级取现状）；模式 D 开头新增代码库可读性自检，读不到则如实告知、不臆测代码行为。
  - **产出物随环境分形**：无代码仓库环境产出自包含的业务语言需求文档，有仓库环境才产出带 file:line 证据的实现层 spec。
  - **白话不失锚**：业务确认单要求每个问题锚定到具体对象（哪张报表/哪个字段/哪句原文），语气通俗但指代精确。
  - **元信息行也是规格**：逆向场景新增第 6 条，原始材料中的"数据范围/备注"类元信息行须与字段清单交叉比对，已回答的问题绝不再问业务。
- **定位放宽（放开入口、锁死动作）**：`description` 与正文第一段不再限定"业务方口述"单一入口——群聊碎片、只写正常流程的模糊 PRD、开发者自己的产品构想、藏在现有 Excel/老系统里的隐性规则均为合法输入；需求来自开发者自己时，开发者兼任需求方角色。README 同步此定位。
- 更新 `SKILL.md` frontmatter 版本号为 `2.3.0`。

## [2.0.0] - 2026-07-16

### Changed

- 同步 skill 核心文件到 v2.0：重构并精简 `SKILL.md` 正文，将铁律升级为"五条铁律"。
- 持久文件结构增加 `parking.md`（需求停车场）。
- `references/chain-audit-checklist.md` 新增 sub-agent 探索回收契约，要求子代理必须带回 `file:line + 原文片段`。
- `scripts/verify_evidence.py` 增加 `【假设·未取证】` 标签识别。
- `templates/questionnaire-template.md` 新增出题规则（依赖剪枝、建议选项分级）。
- 更新 `SKILL.md` frontmatter 版本号为 `2.0.0`。

## [1.12.0] - 2026-07-14

### 初始开源版本

- 将 requirement-clarifier skill 开源到 GitHub。
- 采用 PolyForm Noncommercial License 1.0.0（非商用许可证）。
- 包含核心工作流：新需求澄清、需求变更、上下文维护、链路审计。
- 新增 `scripts/validate_skill.py` 用于 frontmatter 校验。
- 新增 `.github/workflows/release.yml`，发布时自动打包 `.skill` 文件并上传到 GitHub Release。
- 包含盲区检查清单与链路审计清单。
- 包含开发规格、确认单、问卷、上下文模板。
- 明确声明支持所有兼容 OpenCode / Claude Code skill 规范的 AI Agent 平台。
